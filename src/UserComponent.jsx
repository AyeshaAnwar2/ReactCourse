 function Login(){
  return (
    <div>
      <h1>it is login form to check default export</h1>
    </div>
  )
 }
 export default Login;

 export function Profile(){
  return (
    <div>
      <h3>it is function to check the import without default because we can use only one default in one file</h3>
    </div>
  )
 }
